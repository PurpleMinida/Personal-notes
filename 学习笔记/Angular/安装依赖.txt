nodejs 8 10  npm 3+
python 2.x
c++ 编译工具 visual studio（不用安装）     npm install --global --production windows-build-tools

淘宝镜像
cnpm   npm install -g cnpm --registry=https://registry.npm.taobao.org


安装angular脚手架
	cnpm i -g @angular/cli
	检查安装是否 ng --version
卸载angular脚手架
	cnpm uninstall -g @angular/cli



就是安装 
	node 
	npm 
	python 
	npm install --global --production windows-build-tools